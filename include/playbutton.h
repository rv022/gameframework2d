#ifndef __PLAYBUTTON_H__
#define __PLAYBUTTON_H__

#include "mainmenu.h"

/**
 * @brief spawn a new play button button
 * @param position where to spawn it
 * @return NULL on error, or a pointer to the spawned enemy entity
 */
Button *play_new();

#endif
